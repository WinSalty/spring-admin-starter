spring:
  redis:
    host: 127.0.0.1
    port: 6379
    database: 1
    timeout: 3000

logging:
  level:
    root: info
    com.winsalty.quickstart: info

app:
  cors:
    allowed-origins: []
  security:
    jwt-secret: winsalty-quickstart-test-jwt-secret-2026-secure-key
    jwt-expire-seconds: 7200
    register-enabled: false
