spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/spring_admin?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: spring_admin
    password: SpringAdmin@2026
  redis:
    host: 127.0.0.1
    port: 6379
    database: 0
    timeout: 3000

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    banner: false

logging:
  level:
    root: info
    com.winsalty.quickstart: info

app:
  cors:
    allowed-origins:
      - http://localhost:5173
  security:
    jwt-secret: winsalty-quickstart-dev-jwt-secret-2026-secure-key
    jwt-expire-seconds: 7200
    jwt-refresh-expire-seconds: 604800
    register-enabled: true
