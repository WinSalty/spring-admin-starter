spring:
  datasource:
    # 开发环境默认连接本机 MySQL；生产环境不得使用这里的默认密码。
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 生产环境请通过环境变量注入，例如：url: jdbc:mysql://${DB_HOST}:${DB_PORT}/spring_admin?...
    url: jdbc:mysql://${DB_HOST:127.0.0.1}:${DB_PORT:3306}/spring_admin?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: ${DB_USERNAME:spring_admin}
    # 生产环境请通过环境变量注入，例如：password: ${DB_PASSWORD}
    password: ${DB_PASSWORD:SpringAdmin@2026}
    druid:
      # Druid 连接池参数用于控制本地开发和联调期间的数据库连接占用。
      name: DruidPool-SpringAdmin
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 30000
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      validation-query: SELECT 1
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      pool-prepared-statements: true
      max-pool-prepared-statement-per-connection-size: 20
      stat-view-servlet:
        enabled: ${DRUID_STAT_VIEW_ENABLED:false}
      web-stat-filter:
        enabled: false
  redis:
    # Spring Data Redis 与 Redisson 共用该连接配置，避免维护两套 Redis 地址。
    host: ${REDIS_HOST:127.0.0.1}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:}
    database: 0
    timeout: 3000ms
    lettuce:
      pool:
        max-active: 20
        max-idle: 10
        min-idle: 5
        max-wait: 3000ms

mybatis-plus:
  # 数据库字段统一使用下划线，实体统一使用驼峰。
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    banner: false

app:
  cors:
    # 本地前端开发服务器地址。
    allowed-origins:
      - http://localhost:5173
  security:
    swagger-public-enabled: true
    # 生产环境请通过环境变量注入，例如：jwt-secret: ${JWT_SECRET}
    jwt-secret: ${JWT_SECRET:winsalty-quickstart-dev-jwt-secret-2026-secure-key}
    jwt-expire-seconds: 7200
    jwt-refresh-expire-seconds: 604800
    register-enabled: true
  redisson:
    # 开发环境锁等待时间较短，避免本地调试时长时间阻塞。
    lock-wait-seconds: ${REDISSON_LOCK_WAIT_SECONDS:3}
    lock-lease-seconds: ${REDISSON_LOCK_LEASE_SECONDS:30}
    lock-log-enabled: ${REDISSON_LOCK_LOG_ENABLED:true}
