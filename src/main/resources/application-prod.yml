logging:
  level:
    root: info
    com.winsalty.quickstart: info

app:
  cors:
    allowed-origins: []
  security:
    jwt-secret: winsalty-quickstart-prod-jwt-secret-2026-secure-key
    jwt-expire-seconds: 7200
    register-enabled: false
